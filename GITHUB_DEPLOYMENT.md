# 🚀 نشر المشروع عبر GitHub إلى Google Cloud VM

## الفكرة ببساطة:
1. ترفع الكود على GitHub (مجاناً)
2. تدخل على السيرفر (VM) وتنزل الكود من GitHub
3. تشغل المشروع على السيرفر

---

## الخطوات التفصيلية

### الجزء 1: رفع المشروع على GitHub (من جهازك)

```bash
# 1. تهيئة Git في المشروع (إذا لم يكن موجود)
cd /Users/hasanelzein/Desktop/lionbot
git init

# 2. إضافة جميع الملفات
git add .

# 3. إنشاء أول commit
git commit -m "Initial commit - Lion Delivery BOT"

# 4. ربط المشروع مع GitHub (بعد إنشاء repository على الموقع)
# اذهب إلى github.com وأنشئ repository جديد اسمه: lionbot
# ثم نفذ الأمر التالي (استبدل YOUR_USERNAME باسم المستخدم)
git remote add origin https://github.com/YOUR_USERNAME/lionbot.git
git branch -M main
git push -u origin main
```

**ملاحظة مهمة:** ملف `.env` لن يتم رفعه (لأننا أضفناه في `.gitignore`) - هذا للحماية! 🔒

---

### الجزء 2: تنزيل وتشغيل المشروع على السيرفر (VM Instance)

```bash
# 1. الدخول إلى السيرفر عبر SSH
gcloud compute ssh liondelivery --zone=europe-north1-a

# 2. تثبيت Git (إذا لم يكن مثبت)
sudo apt-get update
sudo apt-get install -y git

# 3. تنزيل المشروع من GitHub
git clone https://github.com/YOUR_USERNAME/lionbot.git
cd lionbot

# 4. إنشاء ملف .env يدوياً (لأنه لم يتم رفعه على GitHub)
nano backend/.env
# الصق المحتوى من الملف المحلي، ثم اضغط Ctrl+O ثم Enter ثم Ctrl+X

# 5. تثبيت Docker (إذا لم يكن مثبت)
# انسخ الأوامر من DEPLOYMENT.md

# 6. تشغيل المشروع
sudo docker compose up -d --build

# 7. التأكد من عمل المشروع
sudo docker compose ps
```

---

## لماذا GitHub أفضل؟

✅ **سريع**: لا حاجة لضغط ورفع ملفات ضخمة
✅ **منظم**: يمكنك تحديث الكود بسهولة (git pull)
✅ **آمن**: الملفات الحساسة (.env) لا ترفع
✅ **احترافي**: هذه الطريقة المعتمدة عالمياً

---

## تحديث الكود مستقبلاً

إذا عدلت شيء على جهازك وتريد رفع التعديل:

```bash
# على جهازك المحلي
git add .
git commit -m "وصف التعديل"
git push

# على السيرفر
git pull
sudo docker compose up -d --build
```

سهل جداً! 🎯
